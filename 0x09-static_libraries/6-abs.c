#include"main.h"

/**
 * _abs - calculates the absolute value
 *@num: - undefined data type
 *
 * Return: absolute value of paramete int
 */

int _abs(int num)
{
	if (num < 0)
	{
		return (-num);
	}
	else
	{
		return (num);
	}
}
