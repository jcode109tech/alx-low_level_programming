#include <stdio.h>

/**
 * main - a function that prints out its name.
 * @argc: - count argv
 * @argv: - array
 * Return: Always 0 
*/

int main(int argc, char *argv[])
{
	printf("%s\n", argv);

	return (0);
}
